import { BarExerciseShadowDirective } from './bar-exercise-shadow.directive';

describe('BarExerciseShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new BarExerciseShadowDirective();
    expect(directive).toBeTruthy();
  });
});
